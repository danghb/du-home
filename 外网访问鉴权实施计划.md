# 外网访问鉴权实施计划

状态：待实施  
记录日期：2026-08-14  
实施优先级：照片与家庭状态稳定后进行安全加固

## 1. 背景与当前风险

`duhome.danghb.top` 当前通过 Cloudflare Tunnel 连接 NAS 上的 `family-display` 服务。Tunnel 隐藏了 NAS 的公网入口，但若未配置 Cloudflare Access，知道域名的人仍可直接访问页面及以下隐私接口：

- `/api/v1/dashboard`：家庭备忘、购物清单、天气等聚合信息；
- `/api/v1/status`：房间与设备状态；
- `/api/v1/photos`：照片索引；
- `/media/original/*`：家庭照片原图；
- `/media/thumbnail/*`：照片缩略图。

Cloudflare Zero Trust 免费版当前仍要求提供付款资料。由于暂时没有可用的卡或支付方式，本项目后续采用应用自身鉴权，不依赖 Cloudflare Access。

在鉴权功能完成前，如果不希望上述内容继续公开，应暂时在 Cloudflare Tunnel 中禁用 `duhome.danghb.top` 的 Public Hostname。

## 2. 目标

- 树莓派在家庭局域网内直连 NAS，不需要登录；
- 通过 `https://duhome.danghb.top/` 访问时必须验证密码；
- 页面、API、照片原图和缩略图使用同一套认证；
- 密码不以明文保存；
- 登录状态可长期保持，默认有效期 30 天；
- 登录失败有频率限制，避免暴力尝试；
- 功能默认关闭，避免升级镜像后因配置不完整而锁死访问；
- 不改变 HA Token 只保存在后端的现状。

## 3. 访问架构

### 局域网访问

树莓派使用 NAS 的局域网地址：

```text
http://NAS局域网IP:3000/?orientation=landscape
```

该请求不经过 Cloudflare Tunnel，因此免登录。群晖防火墙必须将 TCP `3000` 限制为仅家庭局域网网段可访问，路由器不得转发此端口。

### 外网访问

```text
浏览器
  → https://duhome.danghb.top
  → Cloudflare Tunnel
  → family-display:3000
  → 应用登录校验
```

在 Cloudflare Tunnel 的 `duhome.danghb.top` Public Hostname 中，将以下选项设置为固定值：

```text
Additional application settings
→ HTTP Host Header
→ external.du-home.internal
```

后端只把 `REMOTE_AUTH_HOST` 对应的请求视为外网入口。直接访问 NAS IP 时 Host 不匹配，因此走局域网免登录路径。

注意：Host 只用于区分局域网入口和 Tunnel 入口，不是认证凭证。真正的认证依靠密码验证和签名会话 Cookie。该设计成立的前提是 NAS 服务没有公网端口映射，外网只能通过 Cloudflare Tunnel 到达。

## 4. 后端实现

### 配置项

计划增加：

```env
REMOTE_AUTH_ENABLED=false
REMOTE_AUTH_HOST=external.du-home.internal
REMOTE_AUTH_PASSWORD_HASH=
REMOTE_AUTH_SESSION_SECRET=
REMOTE_AUTH_SESSION_DAYS=30
REMOTE_AUTH_MAX_ATTEMPTS=5
REMOTE_AUTH_WINDOW_MINUTES=15
```

- `REMOTE_AUTH_ENABLED`：默认 `false`，配置完整后手动启用；
- `REMOTE_AUTH_PASSWORD_HASH`：使用 Node.js `crypto.scrypt` 生成，格式包含随机 salt，不保存明文密码；
- `REMOTE_AUTH_SESSION_SECRET`：至少 32 字节随机值，用于签名会话；
- `REMOTE_AUTH_SESSION_DAYS`：会话有效期，默认 30 天；
- 尝试次数限制按来源 IP 和时间窗口计算。

项目需增加一个本地命令，用于交互式或参数式生成密码哈希与随机会话密钥，避免用户手工拼接格式。

### 请求规则

1. `REMOTE_AUTH_ENABLED=false` 时保持当前行为，便于安全部署与回滚。
2. 请求 Host 不等于 `REMOTE_AUTH_HOST` 时视为局域网直连，不要求登录。
3. 请求 Host 等于 `REMOTE_AUTH_HOST` 时：
   - `/login` 与登录接口允许匿名访问；
   - `/api/v1/health` 保留给容器内部健康检查，不返回隐私信息；
   - 页面请求未登录时跳转到 `/login`；
   - `/api/v1/*` 与 `/media/*` 未登录时返回 `401`；
   - 登录成功后才允许读取页面、API 与照片。
4. 不依据客户端自行提交的 `X-Forwarded-For`、`CF-Connecting-IP` 或私有 IP 请求头绕过认证。

### 会话 Cookie

计划使用无服务端数据库的签名会话 Cookie，至少包含签发时间、到期时间和会话版本，并使用 HMAC 校验完整性。

Cookie 属性：

```text
HttpOnly
Secure
SameSite=Strict
Path=/
Max-Age=配置的会话时长
```

退出登录时立即清除 Cookie。修改密码或会话密钥后，旧会话全部失效。

### 登录保护

- 密码使用恒定时间比较；
- 单个来源默认 15 分钟最多失败 5 次；
- 失败响应不区分“密码错误”和“用户不存在”；
- 日志不得记录密码、Cookie、完整 HA Token 或会话密钥；
- 登录接口只接受 `POST`；
- 登录成功后只允许跳回站内相对路径，避免开放重定向；
- 当前业务接口均为只读；以后增加设备控制或写操作时，需追加 CSRF 防护。

## 5. 前端实现

新增一个与当前 Penpot 风格一致的简单登录页：

- 使用 OPPO Sans、天气氛围背景、现有圆角和颜色变量；
- 仅包含“家庭屏”、密码输入框、登录按钮和错误提示；
- 不展示 HA、房间或照片信息；
- 支持键盘回车提交；
- 不把密码写入 `localStorage`、URL、日志或前端配置；
- 登录成功后返回原访问页面；
- API 返回 `401` 时统一跳转登录页，不反复闪烁或循环请求。

树莓派使用局域网地址，不会看到登录页。

## 6. Cloudflare 与 NAS 配置

### Cloudflare Tunnel

- 保留 `duhome.danghb.top` Public Hostname；
- Origin Service 继续指向 NAS 上的服务；
- 设置 `HTTP Host Header=external.du-home.internal`；
- 不使用公开 Bypass 页面或查询参数密钥；
- 鉴权完成后用移动网络测试所有入口。

### NAS 与路由器

- Docker 端口 `3000` 只供家庭局域网访问；
- 群晖防火墙只允许家庭 LAN 网段访问 `3000`；
- 路由器不配置 `3000`、容器端口或群晖反向代理端口的公网转发；
- Cloudflare Tunnel 是唯一外网入口；
- `.env` 权限限制为仅部署用户和管理员可读。

## 7. 实施顺序

1. 增加后端鉴权模块、登录页、测试和配置校验，但保持 `REMOTE_AUTH_ENABLED=false`。
2. 增加密码哈希/会话密钥生成命令，生成 NAS `.env` 所需值。
3. 构建并部署新镜像，先确认局域网访问不受影响。
4. 在 Cloudflare Tunnel 中设置固定 HTTP Host Header。
5. 在 NAS `.env` 中补齐鉴权配置并改为 `REMOTE_AUTH_ENABLED=true`。
6. 重启服务。
7. 使用移动网络或无痕窗口验证外网登录。
8. 确认树莓派改用 NAS 局域网地址并保持免登录。
9. 检查群晖防火墙和路由器，确保没有公网端口直达服务。

## 8. 验收标准

- 未登录访问 `https://duhome.danghb.top/` 会进入登录页；
- 未登录访问 `/api/v1/dashboard`、`/api/v1/status`、`/api/v1/photos` 返回 `401`；
- 未登录访问任意 `/media/original/*` 或 `/media/thumbnail/*` 返回 `401`；
- 正确密码登录后，页面、API 和图片均正常；
- 错误密码连续尝试达到限制后暂时拒绝继续尝试；
- Cookie 中不包含密码或 HA Token，且具备 `HttpOnly`、`Secure`、`SameSite=Strict`；
- 服务重启后，签名有效且未过期的会话仍然有效；
- 修改 `REMOTE_AUTH_SESSION_SECRET` 后旧会话失效；
- 树莓派通过 NAS 局域网 IP 可正常免登录显示；
- 左右/上下键、页面自动轮换、照片轮换和后台刷新均不受影响；
- Docker 健康检查继续通过；
- 自动化测试覆盖外网 Host、局域网 Host、登录成功/失败、会话过期、API 和媒体文件保护。

## 9. 回滚方案

如果部署后无法登录：

1. 先在 Cloudflare Tunnel 中临时禁用 Public Hostname，避免暴露；
2. 在 NAS `.env` 设置：

   ```env
   REMOTE_AUTH_ENABLED=false
   ```

3. 执行：

   ```bash
   sudo docker-compose up -d --no-build
   ```

4. 通过 NAS 局域网 IP 检查服务；
5. 修复配置后重新启用鉴权和 Tunnel Public Hostname。

## 10. 后续可选升级

将来具备可用支付方式后，可以启用 Cloudflare Access，并选择以下策略之一：

- 仅使用 Cloudflare Access，移除应用密码；
- Cloudflare Access 作为第一层，应用鉴权作为纵深防护；
- 后端验证 `Cf-Access-Jwt-Assertion` 的签名、发行方和应用 AUD。

切换到 Access 前，需要重新评估局域网免登录路径，不能只检查请求头是否存在。
